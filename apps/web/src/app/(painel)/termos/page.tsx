import { PagePlaceholder } from "@/components/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="Termos de Consentimento"
      description="Modelos com preenchimento automático (nome, CPF, procedimento, valor), envio pelo WhatsApp e assinatura pelo celular."
      phase="Fase 2"
    />
  );
}
