import { PagePlaceholder } from "@/components/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="Serviços"
      description="Procedimentos (duração, preço, prazo de retorno, cuidados) e pacotes de sessões — a base da agenda e das automações."
      phase="Em construção — Fase 1"
    />
  );
}
